# Agent / Sales 材料申报与申请表 PDF 设计

## 1. 目标

重构 Agent / Sales 的“材料与补件”模块，使 Agent / Sales 可以先选择名下客户，再按一套完整申报流程一次性上传材料、模拟 OCR、编辑和确认申请资料，最后生成一份可预览、可下载的申请表 PDF，并提交运营审核。

流程参考 `tp-client03.html` 的“首页 / 发起新申报”和申请表 PDF 生成过程，同时吸收 `tp-admin-02.html` 业务准入中按客户类型生成材料清单、显示上传状态和保留审核记录的做法。

## 2. 范围与约束

- 只修改 `demo1` 内的 HTML、CSS、JavaScript 和必要的本地资源。
- 保持 `index.html` 可直接离线打开，不引入远程依赖或后端服务。
- 所有客户、上传、OCR、表单和 PDF 版本状态只保存在浏览器内存。
- 上传文件不发送到外部服务；Demo 不读取文件业务内容，只使用文件名、类型和大小模拟识别。
- 最终 PDF 是根据 OCR 结果和人工编辑表单生成的申请表，不合并原始上传材料。

## 3. 页面结构

“材料与补件”分为两个页面状态：

1. 客户选择页：搜索和选择 Agent / Sales 有权处理的客户。
2. 申报工作页：选择客户后进入完整宽度的五步流程。

工作页顶部固定展示客户名称、客户编号、客户类型、当前申报状态、自动保存时间和返回客户列表入口。切换步骤不会丢失已填写数据。

## 4. 客户选择页

客户列表支持按名称、编号和状态搜索，每条客户记录展示：

- 客户名称和编号
- 个人 KYC 或企业 KYB
- 当前准入或补件状态
- 最近一次材料提交时间
- 当前材料完整度
- 操作：发起申报、继续草稿、处理补件或查看已提交版本

Agent / Sales 只能选择自己负责或已被授权的客户。管理员和运营不会通过此页面代替 Agent / Sales 发起申报。

## 5. 五步申报流程

### 第一步：开始

- 展示客户、申报目的、处理说明和隐私提示。
- 自动生成申请编号，例如 `APP-20260713-018`。
- 用户确认已取得客户授权后进入下一步。

### 第二步：客户与业务

- 根据客户主档预填客户类型和所属 Agent。
- 选择或填写业务类型。
- 个人客户进入 KYC 字段和材料模板；企业客户进入 KYB 字段和材料模板。
- 已有客户资料不会被本次申报直接覆盖，最终提交后形成新的申请版本。

### 第三步：一次性上传与 OCR

- 支持一次选择多个 JPG、JPEG、PNG、PDF 文件。
- 上传后展示文件名、大小、模拟材料分类、处理状态和删除操作。
- 系统根据客户类型生成建议材料清单，并模拟将上传文件归类为身份证明、地址证明、公司注册文件、资金来源等。
- 用户可以人工调整材料分类。
- OCR 状态依次表现为：等待识别、识别中、已提取、需人工确认或识别失败。
- OCR 失败不阻塞流程，用户可选择进入人工填写。

### 第四步：编辑申请表

OCR 提取结果写入可编辑表单。

个人 KYC 至少包含：

- 中文或法定姓名、英文姓名
- 出生日期、国籍或地区
- 证件类型、证件号码、有效期
- 居住地址、电话、电子邮箱
- 业务类型、资金来源和预计业务量

企业 KYB 至少包含：

- 企业法定名称、英文名称
- 注册地区、注册编号、成立日期
- 注册地址、经营地址、联系方式
- 业务类型、资金来源和预计业务量
- 董事与 UBO 摘要

每个 OCR 字段保存置信度。低置信度字段显示明确提示；人工修改后的字段标记“已人工修改”，但不会阻止后续编辑。

### 第五步：确认与生成 PDF

页面集中展示：

- 客户与业务摘要
- 必填字段检查
- 低置信度和人工修改字段
- 已上传材料目录
- Agent / Sales 确认声明

缺少必填字段、必要材料或确认声明时不能生成最终 PDF。

用户确认后生成申请表 PDF。首次生成版本为 `v1`；返回编辑并重新生成时版本递增，旧版本保留在当前会话的版本记录中。

## 6. PDF 内容与行为

申请表 PDF 包含：

1. TP Operations 标识和申请表标题
2. 申请编号、PDF 版本、生成时间和所属 Agent
3. 客户基本资料
4. 业务申请信息
5. OCR 与人工修改说明
6. 已提交材料目录，仅列材料类别和文件名，不嵌入原文件
7. Agent / Sales 确认声明
8. Demo 环境和非正式业务政策提示

PDF 必须是真实生成的 `.pdf` 文件，而不是打印页面或文本文件改后缀。页面通过浏览器 Blob URL 提供内嵌预览和下载。中文内容应正常显示，不出现乱码。

PDF 结果区提供：

- 预览 PDF
- 下载 PDF
- 返回修改
- 提交运营审核
- PDF 版本记录

提交运营后，客户材料状态更新为“待运营审核”，写入客户时间线，并出现在运营处理队列中。

## 7. 补件场景

客户处于“待客户补件”或“合规驳回”时，进入工作页需要：

- 显示补件或驳回原因
- 高亮必须重新提交的材料
- 保留已生效材料，不要求全部重传
- 重新上传后生成新的申请表 PDF 版本
- 提交后回到对应的运营审核环节

## 8. 状态与数据

材料申报状态：

`未开始 → 草稿 → 材料处理中 → 待信息确认 → PDF 已生成 → 待运营审核`

补件状态可以从“待客户补件”进入草稿，重新生成 PDF 后回到“待运营审核”。

每个草稿保存：客户编号、申请编号、当前步骤、上传材料、OCR 字段、人工修改标记、确认声明、PDF 版本和时间线。

## 9. 错误与恢复

- 未选择客户时不能进入申报工作页。
- 未选择文件时不能启动 OCR。
- 不支持的文件格式显示原因，并保留其他有效文件。
- OCR 识别失败时提供“重试识别”和“人工填写”。
- 必填字段缺失时定位到对应字段并说明原因。
- PDF 生成失败时保留草稿，允许重新生成。
- 重复点击生成或提交按钮不会创建重复版本或重复审核事件。
- 无匹配材料分类时标记为“待人工分类”，不能静默忽略。

## 10. 验收标准

- Agent / Sales 可以从材料模块选择名下客户。
- 选择客户后进入独立五步申报工作页。
- 可以一次选择多个文件并看到模拟上传、归类和 OCR 状态。
- OCR 结果进入可编辑的个人或企业申请表。
- 低置信度字段和人工修改字段有清晰标记。
- 信息确认通过后能生成真实、中文可读的 PDF。
- PDF 可以在页面内预览并下载为 `.pdf`。
- 返回修改后可以生成新的 PDF 版本。
- 提交运营后更新状态、时间线和运营队列。
- 补件只要求重传被标记材料，已有有效材料保留。
- 桌面和 390px 手机宽度均可访问所有关键步骤。

## 11. 不包含

- 真实 OCR、文件内容解析、电子签名验证或 PDF 合并。
- 服务器文件存储、跨设备草稿同步或真实消息通知。
- 将 Demo PDF 作为正式合规、开户或银行申请文件使用。
