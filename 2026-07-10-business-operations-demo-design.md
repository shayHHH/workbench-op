# 客户准入与资金运营系统交互 Demo 设计

## 1. 目标

构建一套可直接双击运行的中文交互 Demo，用一条完整案例展示从客户准入到佣金生成的业务闭环。Demo 用于业务澄清和流程评审，不连接真实银行、合规、OCR 或存储系统。

## 2. 技术与交付约束

- 使用原生 HTML、CSS 和 JavaScript，不使用框架、构建工具或远程依赖。
- 主入口为根目录 `index.html`。
- 所有演示数据保存在浏览器内存中，刷新页面时重置。
- 支持主流桌面浏览器，并提供适合平板和窄屏浏览的响应式布局。
- 不采集、上传或持久化真实个人资料。

## 3. 演示角色

Demo 顶部提供角色切换器，不实现真实登录。

| 角色 | 数据范围 | 主要能力 |
| --- | --- | --- |
| 访客/客户 | 当前公开流程 | 查看业务入口、提交资料、查询进度 |
| Agent | 自己名下客户 | 建档、上传材料、补件、预约额度、提交水单、查看佣金 |
| 运营 | 全部待处理申请 | 预审、重复客户处理、案件分配、额度确认、水单匹配 |
| 合规 | 分配给自己的案件 | 风险复核、补件、通过、拒绝、送银行 |
| 管理员 | 全系统 | 查看权限、规则、材料清单和审计记录 |

角色名称代表权限组，不将员工姓名写入权限逻辑。

## 4. 信息架构

### 4.1 全局框架

- 左侧导航根据角色切换模块。
- 顶部包含全局搜索、角色切换、通知和当前用户。
- 首页提供业务队列、关键数量和今日待办，不使用与任务无关的营销指标。
- 所有关键操作均在主内容区完成；抽屉用于查看客户详情和时间线，确认弹层仅用于高风险操作。

### 4.2 功能模块

1. 总览：我的待办、业务队列、临期事项、异常提醒。
2. 客户管理：个人/KYC、企业/KYB、已有客户、关联关系、重复客户提示。
3. 材料与 OCR：文件清单、模拟识别结果、字段置信度、异常项和人工确认。
4. 合规案件：预审、补件、风险复核、通过、拒绝、送银行。
5. 额度预约：创建、确认、修改、取消、到期释放。
6. 水单管理：上传、OCR 摘要、预约匹配、人工纠正、入金确认。
7. 佣金中心：Agent 费率、预计佣金、已确认佣金、待支付佣金和计算依据。
8. 配置与权限：角色矩阵、费率规则、材料模板和流程状态只读示意。
9. 审计日志：按客户展示操作时间、角色、动作、前后状态和备注。

## 5. 主业务流程

主流程使用预置个人客户“陈嘉宁”演示：

1. Agent 创建个人客户，填写基础资料并上传 ID、地址证明和流水。
2. 模拟 OCR 提取姓名、证件号、出生日期和有效期，并标记一项低置信度字段。
3. 系统执行重复客户检查，展示一个同名候选，但使用客户编号、证件掩码和生日进行区分，不自动切换客户。
4. Agent 确认资料并提交，案件状态变为“待运营预审”。
5. 运营发现地址证明缺页，发起补件，状态变为“待客户补件”。
6. Agent 模拟上传补件并重新提交。
7. 运营预审通过并分配给合规。
8. 合规查看规则风险和 OCR 异常，人工确认后批准客户。
9. Agent 为已批准客户预约港币额度。
10. 运营确认额度并锁定至指定日期。
11. Agent 上传水单，系统提取金额、币种、日期和交易编号，并自动建议匹配预约。
12. 运营确认水单与预约匹配，状态变为“入金已确认”。
13. 合规将业务送银行，随后模拟银行完成。
14. 系统按演示费率生成佣金，客户时间线记录完整事件。

每一步完成后显示短暂反馈、更新数字和时间线，并将下一待办转移给对应角色。

## 6. 分支案例

- 企业客户“Northstar Trading Limited”：展示 KYB 材料、董事和 UBO 关系，以及待补件状态。
- 已有客户“林雅雯”：展示追加流水、再次申请额度，不覆盖历史档案。
- 异常水单：金额与预约不一致，必须由运营人工选择“接受差异”或“退回 Agent”。
- 高风险案件：展示合规拒绝路径，拒绝需要填写原因并二次确认。
- 已取消预约：展示额度释放及审计记录。

## 7. 状态模型

### 7.1 客户准入

`草稿 → 待运营预审 → 待客户补件 → 待运营预审 → 待合规审核 → 合规审核中 → 已批准 / 已拒绝`

### 7.2 额度预约

`草稿 → 待确认 → 已锁定 → 已使用 / 已取消 / 已过期`

### 7.3 水单

`待上传 → 待识别 → 待匹配 → 待人工复核 → 入金已确认 / 已退回`

### 7.4 银行处理

`未送银行 → 待送银行 → 银行处理中 → 已完成 / 银行退回`

不同业务对象使用独立状态，客户状态不被水单或额度状态覆盖。

## 8. 关键交互约定

- 客户列表支持状态、类型和负责人筛选以及关键词搜索。
- 客户详情包含概览、材料、申请、额度、水单和时间线六个标签。
- 创建客户使用分步表单；可返回上一步，提交前提供检查摘要。
- 文件上传为模拟操作，选择任意文件后显示上传及 OCR 过程，不读取文件内容。
- 风险结论必须由合规人员人工确认，AI/OCR 只给建议。
- 同名客户只显示脱敏信息，用户必须明确选择客户，不自动轮换。
- 补件必须选择材料和填写说明；拒绝必须填写原因。
- 高影响操作提供确认，普通状态切换不滥用弹窗。
- 页面内提供“重置演示数据”功能，便于重复演示。

## 9. 演示数据与规则

- 预置 3 个主客户和若干队列记录。
- 演示币种为 HKD、USD、CNY。
- 个人客户标准佣金率为 0.35%，企业客户为 0.25%；佣金只在银行完成后确认。
- 预约默认锁定 48 小时，取消或到期后释放额度。
- 所有规则在页面标记为“Demo 假设”，不代表正式业务政策。

## 10. 视觉方向

使用适合办公室日间操作的浅色产品界面。整体采用低饱和暖灰背景、深墨色文字和克制的青绿色业务强调色。风险、等待、成功和异常使用有文字辅助的语义色，不能只依赖颜色传达。信息密度接近正式运营后台，但通过清晰层级和留白避免表格堆叠。

## 11. 错误与空状态

- 必填字段缺失时在字段附近说明原因，并将焦点移到首个错误。
- 无搜索结果时提供清除筛选入口。
- 不符合当前角色的操作不可见或禁用，并说明所需角色。
- OCR 失败、水单无法匹配和材料不完整均提供可继续的人工处理路径。
- 防止重复点击造成同一状态重复流转。

## 12. 验收标准

- 双击 `index.html` 可直接打开，无网络连接也能使用。
- 五种角色可切换，导航和待办随角色变化。
- 主案例可从建档逐步演示到银行完成及佣金生成。
- 至少一个补件、一个重复客户、一个异常水单和一个拒绝分支可操作。
- 列表筛选、搜索、详情标签、状态更新和重置演示数据有效。
- 每次关键动作均写入客户时间线或审计记录。
- 在 1440px、1024px 和 390px 宽度下不出现关键内容不可访问的问题。

## 13. 明确不包含

- 真实用户认证、数据库、文件上传、OCR、制裁名单或银行 API。
- Google Sheet 双向同步。
- 生产环境的数据安全、备份和容灾实现。
- 将 Demo 中的费率、风险或材料规则视为正式政策。
