# TransferEasy 模板 PDF 与逐材料项审核设计

## 1. 目标

升级 Agent / Sales 的材料申报流程，使材料按业务要求逐项上传，并在材料上传完成后由 Agent / Sales 决定是否生成 TransferEasy 申请表。生成申请表时必须使用 `TP/trustpass-stage1-template.pdf` 原始两页模板，并支持客户签名后的 PDF 回传。运营和合规使用同一份材料项清单逐项审核。

## 2. 原始 PDF 模板

- 源文件：`TP/trustpass-stage1-template.pdf`
- 模板共两页，包含 55 个 PDF 表单字段。
- 个人与企业客户暂时共用此模板，并在界面明确标记该模板当前为 Personal Service Application Form。
- 实现方式与 `tp-client03.html` 一致：使用本地 `pdf-lib` 加载模板、填充文本字段和复选框、写入日期与签名，最后扁平化生成 PDF。
- `pdf-lib` 和模板文件复制到 `demo1`，不得依赖 CDN，确保离线打开仍可使用。

## 3. 调整后的业务流程

`选择客户 → 逐材料项上传 → 选择是否生成申请表 → 提交运营`

上传完成后的处理方式有三种：

1. OCR 材料并生成申请表 PDF。
2. 不使用 OCR，手工填写并生成申请表 PDF。
3. 不生成申请表，直接以已上传材料提交运营审核。

三种路径都必须保留选择结果、操作人、时间和材料版本。

## 4. 逐材料项上传

材料上传页不再使用一个多文件篮子，而是按材料要求生成独立材料项。个人客户默认材料项包括：

1. 内地身份证正面
2. 内地身份证反面
3. 护照或 EEP 资料页
4. 手持护照或 EEP 自拍
5. 地址证明
6. 最近银行流水
7. 最近银行月结单
8. 补充材料

企业客户暂时共用申请表模板，但材料项可以按企业场景显示：

1. 公司注册文件
2. 董事与 UBO 名单
3. 董事或授权人身份证明
4. 企业地址证明
5. 公司银行流水
6. 资金来源材料
7. 补充材料

每个材料项独立保存：

- 材料项编号和名称
- 是否必填
- 当前有效文件
- 文件名、格式和大小
- 上传时间与上传人
- 文件版本
- OCR 状态
- 运营审核结论与原因
- 合规审核结论与原因
- 是否需要重新提交

同一材料项重新上传时版本递增，旧版本保留在当前会话的版本记录中。

## 5. 上传后的路径选择

所有必填材料上传完成后显示“后续处理方式”区域。

### OCR 生成申请表

- 对各材料项运行模拟 OCR。
- OCR 结果写入可编辑表单。
- 低置信度字段要求 Agent / Sales 人工确认。
- 生成原模板申请表 PDF。

### 手工填写生成申请表

- 跳过 OCR。
- 打开与原模板字段对应的可编辑表单。
- Agent / Sales 完成必填字段后生成原模板 PDF。

### 不生成申请表

- 不进入申请表编辑步骤。
- 允许直接提交已上传材料给运营审核。
- 提交确认中明确显示“本次未附申请表”。
- 运营和合规可以在审核过程中要求补充申请表，并将申请退回 Agent / Sales。

## 6. 申请表字段映射

可编辑表单应覆盖原模板的核心字段：

- 英文姓名、中文姓名、性别、国籍
- 出生年月日、联系电话
- 职业、居住地址
- 证件类型、证件号码、签发国家
- 财富与资金来源
- 预计服务活动和交易用途
- 服务目的地
- 年度预计金额与单笔预计金额
- 申请人签名和签署日期

字段名称、单选项、复选框和金额区间应尽量与 `tp-client03.html` 现有映射保持一致。

## 7. 未签名与已签名 PDF

生成申请表后，系统创建“待客户签名 PDF”。Agent / Sales 可以：

- 在系统内预览
- 下载给客户签名
- 重新编辑并生成新版本
- 上传客户签名后的 PDF

签名回传区保存：

- 对应未签名 PDF 版本
- 已签名文件名、大小和上传时间
- 上传人
- 当前有效状态

只有上传已签名 PDF 后，系统才显示“申请表签署完成”。但如果 Agent / Sales 选择“不生成申请表”，则不要求签名 PDF，也允许直接提交运营。

## 8. 客户签署链接示意

当前离线 Demo 不能生成真正跨设备访问的分享链接。页面可以保留“生成客户签署链接”入口作为生产能力示意，并展示：

- 示例链接
- 有效期
- 签署状态
- 重新生成入口

示例必须明确标记为不可外部访问的 Demo，不使用 `blob:` 地址冒充分享链接。

## 9. 运营与合规审核

运营和合规在案件工作区看到同一套材料项清单。每一项展示：

- 当前有效文件和版本
- 预览与下载
- OCR 状态
- Agent / Sales 备注
- 本角色的审核结论

运营可以逐项通过或退回，选择多个退回材料并生成补件通知。运营完成材料预审后提交合规。

合规查看运营已通过的材料和相同文件版本，逐项记录合规结论。合规退回时案件返回运营的“合规驳回”队列，再由运营通知 Agent / Sales。

如果本次选择“不生成申请表”，审核区显示“未附申请表”。运营或合规仍可继续审核，也可以将“申请表 PDF”作为新增补件要求退回。

## 10. 状态

材料申报状态：

`草稿 → 材料已上传 → 路径已选择 → 待客户签名（可选）→ 待运营审核`

申请表状态：

`不生成 / 编辑中 / 待客户签名 / 已签名 / 已作废`

材料项审核状态：

`未上传 / 待审核 / 运营通过 / 运营退回 / 合规通过 / 合规退回`

## 11. 错误与边界场景

- 必填材料项未上传时不能选择后续路径。
- 不支持的文件格式只影响当前材料项，不清空其他材料。
- OCR 失败时允许改用手工填写。
- 生成模板 PDF 失败时保留表单和材料状态。
- 已签名 PDF 必须关联一个未签名版本。
- 未生成申请表直接提交时必须二次确认。
- 同一材料项重复上传不会覆盖历史版本。
- 运营和合规不能审核不同的材料版本；提交合规时保存材料快照。

## 12. 验收标准

- 页面按多个材料项分别上传文件。
- 材料上传后才显示后续路径选择。
- OCR、手工填写、不生成申请表三条路径均可操作。
- 生成的 PDF 保持原 TransferEasy 两页模板版式。
- PDF 字段、复选框、日期和签名位置正确。
- 可以预览和下载未签名 PDF。
- 可以上传已签名 PDF 并关联原版本。
- 不生成申请表时可以直接提交运营。
- 运营和合规可以逐项预览、下载、通过或退回材料。
- 补件要求能够精确指向材料项或申请表 PDF。
- 所有关键动作进入客户时间线和操作记录。
- 页面在桌面和 390px 手机宽度可访问。

## 13. 不包含

- 真实 OCR、在线电子签名或跨设备文件分享。
- 真实后端文件存储和可公开访问链接。
- 企业专用 PDF 模板；企业客户本轮暂时共用个人服务申请表。
